{
  "name": "persona-analyzer-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @workspace/api-server run dev",
    "build": "pnpm -r build",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
